#include <studio.h>;

int main{void} {
    int x;

    printf("Dear user!");
    printf("Enter a number, I will print its cube and add four!");
    scanf("%d", x);
    x = x*x*x + 4;
    printf("x square is: %f", x);
}
